# Supabase (obrigatório)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anon

# Auth: o app usa Supabase Auth (signInWithPassword). Não use segredos no frontend:
# - NÃO use VITE_EDGE_FUNCTION_SECRET (tudo com VITE_ é público)
# - NÃO use VITE_SUPABASE_JWT_SECRET (JWT secret nunca no frontend)
# O cliente envia o JWT da sessão automaticamente; RLS usa auth.uid() no servidor.
